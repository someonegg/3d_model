# 键盘解压按键器 · FDM

独立设计的方形单键解压器：方形键帽、凸轮段落弹舌、独立回弹片和钥匙圈挂孔。六个零件全部用 PLA 打印，不需要键盘轴、螺丝或金属弹簧。

![装配外观](preview.png)

| 参数 | 尺寸 / 配置 |
| --- | --- |
| 底座主体 | 32 × 32 mm，圆角 |
| 含挂耳外廓 | 39 × 32 mm |
| 装配高度 | 约 25 mm |
| 键帽 | 24 × 24 mm，边缘收坡 |
| 机械限位行程 | 3 mm |
| 挂孔 | 直径 4 mm，挂耳厚 5 mm，最窄孔边 2.4 mm |
| 导向间隙 | 单边 0.25 mm；一角削角防反装 |
| 回弹片 | 弹臂厚 1.2 mm、宽 3.2 mm，外框厚 1.8 mm，预压 0.3 mm |
| 段落弹舌 | 平面内弯曲，臂宽 1.2 mm、厚 1.6 mm，连续定位边框总高 2.7 mm |

## 本次结构修订

针对旧版弹力不足及薄片难装，增加弹臂截面和预压，并加厚回弹外框；段落件使用连续定位边框替代四个小脚，导向孔入口增加导入斜面，段落外框增加自由端避让。底盖同步降低支柱并增加弹臂避让槽。六件数量、外廓与 3 mm 行程保持不变。

按相同 2500 MPa 弹性模量的梁模型估算，回弹片初始推力从约 0.026 N 增至 0.356 N，按到底从约 0.82 N 增至 3.91 N；段落臂侧向刚度约为旧版的 6.75 倍。数值不包含打印各向异性、摩擦与长期蠕变，不等于实测按压力。回弹臂估算峰值应变约 1.83%，新版必须重新进行白化、复位和耐久试打。

**升级时建议重打整套六件；仅键帽与配合试片保持原样可复用。** 新弹片、导向柱及底盖需配套使用，不要直接装进旧版底盖堆叠。

## 先打印配合试片

先下载 [配合试片](fit-coupon.stl)，包含一个双孔量规和一个带插接头的试柱。长孔检查导向滑动，小盲孔检查键帽插接。

试柱的削角应对齐长孔削角，能用手推过、不明显卡滞。小插接头有微量过盈筋，插入小孔后应能保持，不能靠强压导致开裂。先清理象脚和接缝；仍不合适时调整源码的导向间隙或插接筋并重新生成，不要整体缩放模型。过盈筋可以少量打磨。

## 打印文件与设置

- [全部六件 · 打印排版](all-parts.stl)：约 118 × 76 mm，按 100% 比例导入 Bambu Studio；保持各件相对位置及既定朝向。
- 独立文件：[外壳](shell.stl)、[键帽](keycap.stl)、[导向柱](plunger.stl)、[滑动底盖](base.stl)、[备用回弹片](return-spring.stl)、[备用段落弹舌](click-spring.stl)。颜色自由，键帽可以单独换色打印。
- 选择 **拓竹 P2S、0.4 mm 喷嘴、PLA**；首层和后续层高均为 **0.20 mm**，3 圈墙，15% 填充，Arachne 墙生成器，关闭支撑。外墙速度 45 mm/s、内墙 80 mm/s 作为起点，材料温度沿用所用 PLA 配置。
- 外壳顶面、键帽顶面、导向柱插接头端、两个弹性件的平整弹臂面朝下；底盖平放。回弹外框的加厚凸边朝上，段落件的定位边框朝上。不要自动定向。
- 回弹臂为 6 层，段落臂为 8 层；段落触头接触区局部保留 4 层，以保证凸轮及时释放。检查两条长回弹臂及 1.2 mm 段落臂连续成形；不要把外壳或键帽的填充比例套用为“弹臂内部可以缺料”。
- 局部悬挑包括导向柱防脱肩、凸轮坡面和底盖导轨；切片验证记录见 [切片报告](slicing-validation.json)。
- 打印完成冷却后取件。尤其清理弹舌与敲击止挡之间约 0.35 mm 的间隙；若被挤出线黏连，不要强行掰开，应先调整流量或增大止挡间隙后重打该件。

用料、耗时、弹性件路径检查及软件诊断见 [切片报告](slicing-validation.json)。重建后切片状态重置为 `not-run`。

## 装配

底盖四个支柱的迎入侧现增加水平长 1.2 mm、高差 0.8 mm 的导入斜坡，滑入时将回弹片外框逐渐推到位；支撑高度保持 5.3 mm，每个支柱保留 0.8 × 2 mm 平顶。针对“需要手压回弹片边缘才能滑入”的反馈，只修改底盖，可保留其余五件，仅补打 [滑动底盖](base.stl)。回弹片厚度、预压、导轨及卡扣尺寸不变。

![六件爆炸图](exploded.png)

爆炸图从上到下依次为键帽、导向柱、外壳、段落弹舌、回弹片、底盖。图中弹性件显示未受力状态。

1. 外壳翻面，开口朝上。将导向柱的插接头从壳内穿过长导向孔；削角只有一个正确方向，两个防脱肩留在壳内。
2. 放入段落弹舌：连续定位边框朝壳底，框架缺口对准挂耳侧内壁的定位筋。两个向内伸出的触头对准导向柱两侧凸轮。
3. 放入回弹片：框架缺口同样对准定位筋。加厚外框凸边朝壳底。捏住外框两侧放入，不要捏弹臂；两条长弹臂的自由端分别接触导向柱底部两个小推脚，不能让推脚压到框架或弹臂根部。
4. 扶正两层外框，使缺口都落入定位筋；不要压住中间弹臂。底盖四个支柱朝内，从壳侧较大的入口沿两边导轨缓慢滑入。支柱斜坡应逐渐将回弹外框推到位，无需另用手压住边缘；最后由平顶托住框架，底盖两侧弹性舌的小凸点进入定位凹位。若遇到突然卡死或需明显用力，退出检查毛边、外框倾斜和缺口位置，不要强推。
5. 翻回正面，扶住导向柱，将键帽压到插接肩。先缓慢按几次，确认能复位，再试中心与四角按压。
6. 拆卸时从侧面指甲槽拉出底盖，托住内部零件；无需破坏卡扣。钥匙圈最后再安装，挂环不包含在打印文件中。

## 工作方式与试打验收

按下时，两个推脚只加载回弹片自由端；凸轮同时向外推开两侧弹舌。接近行程末段，凸轮回落，弹舌释放；其回摆可能敲击相邻止挡，形成咔哒反馈。回弹片负责把键帽推回，壳顶和防脱肩分别限制下压及上抬。

几何验收覆盖静态装配、底盖滑入路径及每 0.25 mm 的按压位置，使用梁挠曲近似检查弹性件避让。悬臂初步筛查使用端部载荷梁模型，依据 [MIT 的悬臂梁课程资料](https://ocw.mit.edu/courses/3-052-nanomechanics-of-materials-and-biomaterials-spring-2007/d8d2ffa0503ad0adf0edb741ddb4e4c8_lec2_appendix.pdf)；取保守有效长度和假设弹性模量 2500 MPa，估算弹性应变与回复力。具体数值见 [几何与运动报告](detail-validation.json)。

底盖专项验收另覆盖回弹外框向壳底偏离最终位置 0.3 mm、0.45 mm 的情况，以 0.1 mm 滑入步长，从最终 STL 的接触面计算外框逐步归位，并检查弹臂及周边零件避让。此近似假设外框保持水平，不模拟摩擦、倾斜或手推力。

首次试打按以下标准记录结果：

- 中心及四角按压均不卡滞，松手能回到上端；底盖不会自行滑开。
- 能感觉到段落，并听到可辨的咔哒声。若没有声音，应记录为发声目标未通过。
- 连续按压 200 次后检查回弹片和弹舌的根部，不应出现白化、裂纹、持续塌陷或明显松动。
- 新底盖连续装拆 10 次，应无需手压回弹外框，无刮伤、白化或闭合松动；复查按压手感和复位。此项实物验证尚未完成。
- 回弹不足时检查导向摩擦、弹片方向和毛边，并记录松手后的残余行程；不得仅凭几何校验认定弹力合格；有永久变形时更换回弹片。不要把受损弹片热弯后继续用。

## 验收状态

在前次加厚修订基础上，本次处理底盖支柱顶住回弹外框的实物反馈。通用几何、归位路径及完整按压行程检查通过；旧直角底盖会被新增归位检查拒绝。P2S、0.4 mm 喷嘴、0.2 mm 层高切片完成，四个支柱的斜坡逐层收窄及平顶挤出路径检查通过；切片器退出码为 0，日志中的 `Invalid T command (T65535)` 诊断保留在 [切片报告](slicing-validation.json)。新版实物装拆与试打尚未完成。

## 维护与重建

`src/build.py` 使用独立几何生成逻辑，`src/verify.py` 检查最终 STL。配合试片与正式零件共用导向和插接几何。修改厚度或层高时必须同时核对装配堆叠高度、预压、运动间隙与切片路径。

```sh
npm run models:build -- keyboard-fidget-fdm
.venv/bin/python models/keyboard-fidget-fdm/src/slice.py
npm run models:validate -- keyboard-fidget-fdm
npm run build
```

`src/slice.py` 是本机 Bambu Studio 可选检查，使用安装的 P2S 配置，临时 3MF、G-code 和切片图保留在 `tmp/keyboard-fidget-slicing/` 供复核。重建几何会把切片状态重置为未运行，避免沿用旧报告。报告记录输入 STL 哈希、软件版本和当次运行诊断。

本次改版的旧版 STL 与装配数据保留在本地 `tmp/keyboard-fidget-baseline/`，用于核对修改范围；新版切片工程及弹臂逐层路径图保留在 `tmp/keyboard-fidget-slicing/`。

本次底盖斜坡修改前的基线与范围比较结果另保留在 `tmp/keyboard-fidget-ramp-baseline/`：其余五件和配合试片文件一致，底盖仅在四处斜坡范围内减少约 3.84 mm³ 材料。此次切片工程、弹臂及支柱逐层路径图保留在 `tmp/keyboard-fidget-ramp-slicing/`，其中 `support-toolpaths.png` 展示四个支柱在 Z=4.6、4.8、5.0、5.2 mm 的路径。
